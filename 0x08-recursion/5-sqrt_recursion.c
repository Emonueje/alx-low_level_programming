#include "main.h"

/**
 * _sqrt_recursion - finds the sqrt of input
 * @n: input of type int
 * Return: the sqrt of the number
 */
int _sqrt_recursion(int n)
{

	if ((n / (n - 1) == (n - 1))
		return (n);
	else
		return (i / (_sqrt_recursion(n - 1)));
	return (-1);
}
