#include "main.h"

/**
 * mul - return the product of two numbers
 * @a: first number
 * @b: second numer
 * Return: Returns the product
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
